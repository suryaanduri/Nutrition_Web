import { Routes } from '@angular/router';
import { AppShellLayoutComponent } from './core/layout/app-shell-layout.component';

export const routes: Routes = [
  {
    path: '',
    component: AppShellLayoutComponent,
    title: 'NCM Platform | Admin Shell'
  }
];
